{
  "name": "tanstack-shared-routes-monorepo",
  "private": true,
  "workspaces": [
    "workspaces/*"
  ],
  "type": "module",
  "scripts": {
    "build": "bun run --filter tanstack-shared-routes build",
    "test": "bun run --filter tanstack-shared-routes test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
