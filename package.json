{
  "name": "tanstack-shared-routes-monorepo",
  "private": true,
  "workspaces": [
    "workspaces/*"
  ],
  "type": "module",
  "scripts": {
    "build": "bun run --cwd workspaces/tanstack-shared-routes build",
    "test": "bun run --cwd workspaces/tanstack-shared-routes test",
    "typecheck": "bun run --cwd workspaces/tanstack-shared-routes typecheck && bun run --cwd workspaces/basic-start-app typecheck"
  }
}
