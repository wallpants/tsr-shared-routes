# tsr-shared-routes

Mount an already-mounted TanStack Router route subtree at multiple paths.

Full documentation: [repository README](../../README.md). (The publish step copies it into the package so npm shows the real docs.)
